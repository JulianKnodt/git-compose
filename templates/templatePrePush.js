const reporter = require('../utils/report.js');
module.exports = (options, localHead, localSHA, remoteHead, remoteSHA) => {
  
}
module.exports.options = {
  error: {
    expecting: 0,
    default: false
  }
}
